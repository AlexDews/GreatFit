/**
 ** LuminaNexus ModuX Spollers Configuration
 */
export const SPOLLER_DEFAULTS = {
  speed: 300,                     // Скорость анимации в мс
  activeClass: '_spoller-active', // Класс активного состояния
  //~ Глобальный переключатель режима аккордеона
  // true  — работает как аккордеон (при открытии одного другие закрываются)
  // false — обычные спойлеры (можно открыть хоть все одновременно)
  oneSpoller: true,
};